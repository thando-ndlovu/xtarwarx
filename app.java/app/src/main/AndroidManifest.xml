<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xyclonedesigns.xtarwarx">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/XtarWarx.Theme">
        <activity
            android:name=".activities.TabLayoutActivity"
            android:theme="@style/XtarWarx.Theme.TabLayout"/>
        <activity
            android:name=".activities.SettingsActivity"
            android:theme="@style/XtarWarx.Theme.Settings"/>
        <activity
            android:name=".activities.SplashScreenActivity"
            android:enabled="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:theme="@style/XtarWarx.Theme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>